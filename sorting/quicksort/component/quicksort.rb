class Array
  def quicksort
    return self if self.length < 2
    arr1 = Array.new
    arr2 = Array.new
    pivot = self[self.length / 2]

  end

  private
  def partition(arr, left, right)
    
  end
end
